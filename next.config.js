/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  compiler: { 
    // React Compiler disabled (already decided)
    // optional: remove if using default
  },
}

module.exports = nextConfig
